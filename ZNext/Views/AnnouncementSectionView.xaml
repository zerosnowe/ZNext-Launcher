<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ZNext.Views.AnnouncementSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <StackPanel Spacing="16">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                <StackPanel Spacing="4">
                    <TextBlock Text="系统公告" FontSize="28" FontWeight="SemiBold"/>
                    <TextBlock x:Name="AnnouncementStatusText"
                               x:FieldModifier="public"
                               Text="点击右上角公告按钮加载"
                               FontSize="13"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
                <Button Grid.Column="1"
                        Content="刷新公告"
                        Height="36"
                        VerticalAlignment="Center"
                        Click="RefreshAnnouncementButton_Click"/>
            </Grid>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <RichTextBlock x:Name="AnnouncementRichTextBlock"
                                   x:FieldModifier="public"
                                   IsTextSelectionEnabled="True"
                                   TextWrapping="Wrap"
                                   IsColorFontEnabled="True"/>
                </ScrollViewer>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

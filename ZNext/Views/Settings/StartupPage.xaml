<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ZNext.Views.Settings.StartupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border x:Name="RootBorder"
                Margin="30,0,30,24">
            <StackPanel MaxWidth="1000"
                        HorizontalAlignment="Stretch"
                        Spacing="4">
                <controls:SettingsCard Header="开机自启动"
                                       Description="允许跟随账户登录运行">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon FontFamily="{ThemeResource AppSymbolFontFamily}"
                                  Glyph="&#xE7E8;"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch x:Name="AutoStartToggle"
                                  IsOn="{Binding IsAutoStartEnabled, Mode=TwoWay}"
                                  OffContent="关闭"
                                  OnContent="开启"
                                  Toggled="AutoStartToggle_Toggled"/>
                </controls:SettingsCard>

                <controls:SettingsExpander Header="隧道随应用启动"
                                           Description="跟随应用自动启动隧道"
                                           IsExpanded="False"
                                           x:Name="AutoStartTunnelsExpander">
                    <controls:SettingsExpander.HeaderIcon>
                        <FontIcon FontFamily="{ThemeResource AppSymbolFontFamily}"
                                  Glyph="&#xE7B5;"/>
                    </controls:SettingsExpander.HeaderIcon>
                    <controls:SettingsExpander.Content>
                        <ToggleSwitch x:Name="AutoStartTunnelsToggle"
                                      IsOn="{Binding IsAutoStartTunnelsEnabled, Mode=TwoWay}"
                                      OffContent=""
                                      OnContent=""
                                      Toggled="AutoStartTunnelsToggle_Toggled"/>
                    </controls:SettingsExpander.Content>
                    <controls:SettingsExpander.ItemsHeader>
                        <Grid Padding="24,0">
                            <TextBlock x:Name="AutoStartTunnelListStatusText"
                                       Text=""
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </controls:SettingsExpander.ItemsHeader>
                </controls:SettingsExpander>
            </StackPanel>
        </Border>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutVisualStates">
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="641"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootBorder.Margin" Value="20,0,20,20"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </ScrollViewer>
</Page>
